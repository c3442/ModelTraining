# PyTorch & core vision
# torch==2.4.0
# torchvision==0.19.0
tensorboard

# Transformers (Gemma release branch compatible)
transformers>=5.0.0rc0
timm

# Hugging Face ecosystem
huggingface_hub
datasets==3.3.2
accelerate==1.4.0
evaluate==0.4.3
trl==0.15.2
peft==0.14.0

# QLoRA / 4-bit quantization (A100 / CUDA only)
bitsandbytes==0.45.3

# Flash Attention 2 (A100 optimized)
# flash-attn==2.8.3

# Vision & image processing
pillow==11.1.0

# Tokenization & serialization
sentencepiece
protobuf

# S3 + filesystem support (needed for your image + JSONL loading)
fsspec
s3fs
